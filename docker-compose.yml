services:
  utility-bot:
    build: .
    container_name: utility-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./credentials.json:/app/credentials.json
    working_dir: /app